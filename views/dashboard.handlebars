<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Dashboard - Blog</title>
  <link rel="stylesheet" href="../public/css/styles.css"> 
</head>

<body>
  <header>
    <h1>Your Dashboard</h1>
    <nav>
      <a href="/logout">Logout</a>
    </nav>
  </header>
  <main>
    <section class="dashboard-actions">
      <button onclick="location.href='/posts/new'">Create New Post</button>
    </section>
    <section class="user-posts">
      {{#if posts.length}}
      <h2>Your Posts</h2>
      {{#each posts}}
      <article class="post">
        <h3>{{this.title}}</h3>
        <p>{{this.content}}</p>
        <p>Published on: {{formatDate this.createdAt}}</p>
        <button onclick="location.href='/posts/edit/{{this.id}}'">Edit</button>
        <button onclick="handleDelete('{{this.id}}')">Delete</button>
      </article>
      {{/each}}
      {{else}}
      <p>You haven't posted anything yet.</p>
      {{/if}}
    </section>
  </main>
  <script>
    function handleDelete(postId) {
      // Implement the delete logic here.
      // For example, sending a fetch request to your API endpoint to delete the post and then reloading the page or removing the post element from the DOM.
      console.log("Delete post with ID:", postId);
    }
  </script>
</body>

</html>